// Site navigation. `key` looks up the label in the active locale (nav.*).
// Standalone routes — every nav spot (topbar, hero, bottom) renders this
// same list, so adding a page here is the only step needed to link it
// everywhere.
export const pageLinks = [{ href: '/about', key: 'about' }];
